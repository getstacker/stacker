namespace ''

task 'somecmd', desc: 'testing utils module commands', ->
  sh 'ls -la && echo "done"'
  sh 'echo "!!!!!! hi"'
  # throw 'error inside somecmd task'
  1234


task 'cmd2', ->
  throw 'error inside cmd2 task'
  'cmd2 return'

